# Apply evolution plan

You are applying an already-approved evolution plan. Follow it exactly.

1. **Scope**: Change only what the plan describes. Do not add unrelated changes.
2. **Execute**: For each item in the plan's `changes`, perform the stated action (create/edit/delete/command).
3. **Verify**: Run each command in `verification_commands` (e.g. `cargo fmt`, `./tests/architecture.sh`) and note the result.
4. **Report**: Summarize what was changed, verification results, and any remaining issues.

Reply in plain language. No special markers required.
