[package]
name = "plugins"
version = "0.1.0"
edition = "2021"
description = "MCP/stdio implementations. Phase9 placeholder."

[lib]
path = "src/lib.rs"

[dependencies]
common = { path = "../../core/common" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
toml = "1.0"
thiserror = "1.0"

[dev-dependencies]
tempfile = "3"
